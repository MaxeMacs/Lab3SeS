import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Test gol pentru a evita erorile', (WidgetTester tester) async {
    // Nu testăm nimic deocamdată
    expect(true, true);
  });
}